----------------------------------------------------------------------------------------
v1:
----------------------------------------------------------------------------------------
w = int(input())
print(("YES", "NO")[w%2 or w<3])  # тернарный оператор, с использованием кортежа
                                  # Шаблон:
                                  # (if_test_is_false, if_test_is_true)[test]
               
               
               
----------------------------------------------------------------------------------------
v2:
----------------------------------------------------------------------------------------
print("NO" if max(3, int(input())) & 1 else "YES") # используется свойство бинарного представления чисел
                                                   # четные оканчиваются на 0, нечетные - на 1
                                                   # + условие, что число не меньше 3 (если меньше, заменяем нечетным)
----------------------------------------------------------------------------------------
